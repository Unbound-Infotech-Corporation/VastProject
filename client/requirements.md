## Packages
date-fns | Formatting log timestamps
lucide-react | Already installed but confirming for technical icons
clsx | Utility for conditional class merging
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config Assumptions:
We are using the default tailwind configuration but overriding the base CSS variables in index.css to enforce a strict, premium dark mode "terminal/server" aesthetic.
The application is designed to be exclusively dark mode.
