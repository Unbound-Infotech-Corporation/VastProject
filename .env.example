# Optional for local dashboard development. Production deploy.sh generates secrets.
DATABASE_URL=postgresql://vastai_user:change-me@localhost:5432/vastai_optimizer
NODE_ENV=development
PORT=5000

# Required for POST /api/optimize in production. Never commit the real value.
OPTIMIZER_API_TOKEN=generate-with-openssl-rand-hex-24
